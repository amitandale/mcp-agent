from mcp_agent.app import MCPApp

app = MCPApp(
    name="implementation_workflow_v1",
    description=(
        "Implements the PR Implementation Workflow V1 end-to-end and exposes it as an MCP workflow tool."
    ),
)
