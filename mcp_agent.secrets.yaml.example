# Example secrets configuration for the VibeCoding PR Orchestrator.
# Copy this file to `mcp_agent.secrets.yaml` and populate the required tokens.

secrets:
  github:
    token: "ghp_your_token_here"
  sourcerer:
    api_key: "sourcerer-api-key"
  dependency-management:
    api_key: "dependency-management-api-key"
